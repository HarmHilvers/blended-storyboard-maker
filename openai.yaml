interface:
  display_name: "Blended Storyboard"
  short_description: "Build an interactive blended storyboard"
  default_prompt: "Use $blended-storyboard-maker to turn the user-provided course materials into a single interactive HTML storyboard."
